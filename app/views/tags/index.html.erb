<%= content_for(:page_title) { t('.page_title') } %>
<div class="tag-list">
  <div class="col s10">
    <% @tags.each do |tag| %>
      <div class="tag tag-individual">
        <%= link_to tag, tasks_url(tag: tag) %>
      </div> <br>
    <% end %>
  </div>
</div>

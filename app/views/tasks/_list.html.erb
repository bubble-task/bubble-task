<div class="task-summary-list">
  <% tasks.each do |task| %>
    <%= link_to task_url(task), class: 'collection-item' do %>
      <%= task.title %><i class="material-icons left">account_circle</i>
    <% end -%>
  <% end %>
</ul>

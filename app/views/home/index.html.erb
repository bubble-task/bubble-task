<%= content_for(:page_title) { 'Home' } %>
<% @tasks.each do |task| %>
  <%= link_to task.title, task_url(task) %>
<% end %>
<div id="new-task-fab" class="fixed-action-btn">
  <a class="btn-floating btn-large">
    <i class="large material-icons">mode_edit</i>
  </a>
</div>

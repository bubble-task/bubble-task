<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <% kind = name.to_s == 'notice' ? 'success' : 'error' %>
    <%= javascript_tag("Materialize.toast('#{msg}', 30000, '#{kind}')") %>
  <% end %>
<% end %>
